<h1 class="heading">task一覧</h1>

<%= link_to 'タスク登録', new_task_path %>

<% @tasks.each do |t| %>
  <div class="task">
    <div class="task_date">
      <%= t.created_at %>
    </div>
    <div class="task_title">
      <%= t.title %>
    </div>
  </div>
<% end %>